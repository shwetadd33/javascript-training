const firstName = 'Shweta';
const lastName = 'Deshmukh';
const birthYear = 1993;

const shweta = "I'm " + firstName + ' ' + lastName;
console.log(shweta);

// Let's use template literal to print the string in more convenient way
const shwetaNew = `I'm ${firstName} ${lastName}, and birth year ${birthYear}`;
console.log(shwetaNew);

// To write multi line text

console.log(`String with
multiple 
lines`);

